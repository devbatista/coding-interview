<div class="card shadow-sm mb-4">
  <div class="card-body">
    <h2 class="card-title"><%= @company.name %></h2>
    <div class="mt-4">
      <%= link_to 'Edit', edit_company_path(@company), class: "btn btn-warning me-2" %>
      <%= link_to 'Back', companies_path, class: "btn btn-secondary" %>
    </div>
  </div>
</div>